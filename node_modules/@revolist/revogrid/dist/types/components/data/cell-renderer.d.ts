import { type VNode, EventEmitter } from '../../stencil-public-runtime';
import { DragStartEvent, CellTemplateProp } from "../../types/index";
interface RenderProps {
    schemaModel: CellTemplateProp;
    additionalData?: any;
    dragStartCell?: EventEmitter<DragStartEvent>;
}
export declare const CellRenderer: ({ renderProps, cellProps, }: {
    renderProps: RenderProps;
    cellProps: any;
}) => VNode;
export {};
