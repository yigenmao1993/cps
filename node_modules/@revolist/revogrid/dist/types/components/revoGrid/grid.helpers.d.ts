import { RowDefinition } from "../../types/index";
export declare const rowDefinitionByType: (newVal?: RowDefinition[]) => Partial<{
    rowPinStart: {
        sizes?: Record<number, number>;
    };
    rowPinEnd: {
        sizes?: Record<number, number>;
    };
    rgRow: {
        sizes?: Record<number, number>;
    };
}>;
export declare const rowDefinitionRemoveByType: (oldVal?: RowDefinition[]) => Partial<{
    rowPinStart: number[];
    rowPinEnd: number[];
    rgRow: number[];
}>;
