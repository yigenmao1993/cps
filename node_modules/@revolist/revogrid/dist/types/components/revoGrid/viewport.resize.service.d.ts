type ResizeEntry = {
    width: number;
    height: number;
};
export default class GridResizeService {
    private readonly resize;
    private resizeObserver;
    private previousSize;
    readonly apply: import("lodash").DebouncedFunc<(e: {
        width: number;
        height: number;
    }) => void>;
    constructor(el: HTMLElement, resize: (entry: ResizeEntry, previousSize: ResizeEntry) => void, elements: (HTMLElement | undefined)[]);
    init(el: HTMLElement, extras?: HTMLElement[]): void;
    destroy(): void;
}
export {};
