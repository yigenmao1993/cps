import { EventEmitter, type VNode } from '../../stencil-public-runtime';
/**
 * VNode to html converter for stencil components.
 * Transform VNode to html string.
 */
/**
 * @internal
 */
export declare class VNodeToHtml {
    redraw: (() => VNode[]) | null | undefined;
    html: EventEmitter<{
        html: string;
        vnodes: (VNode[]) | null;
    }>;
    el: HTMLElement;
    private vnodes;
    componentDidRender(): void;
    render(): any;
}
