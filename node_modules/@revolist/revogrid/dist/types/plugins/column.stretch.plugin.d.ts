import { BasePlugin } from './base.plugin';
import { DimensionCols, PluginBaseComponent, PluginProviders, ColumnRegular } from "../types/index";
export declare class StretchColumn extends BasePlugin {
    providers: PluginProviders;
    private stretchedColumn;
    private readonly scrollSize;
    constructor(revogrid: HTMLRevoGridElement, providers: PluginProviders);
    private setScroll;
    private activateChanges;
    private dropChanges;
    private apply;
    /**
     * Apply stretch changes
     */
    applyStretch(columns: Record<DimensionCols, ColumnRegular[]>): void;
}
/**
 * Check plugin type is Stretch
 */
export declare function isStretchPlugin(plugin: PluginBaseComponent | StretchColumn): plugin is StretchColumn;
