import { DataType } from "../../types/index";
export declare function doCollapse(pIndex: number, source: DataType[]): {
    trimmed: Record<number, boolean>;
};
/**
 *
 * @param pIndex - physical index
 * @param vIndex - virtual index, need to update item collection
 * @param source - data source
 * @param rowItemsIndexes - rgRow indexes
 */
export declare function doExpand(vIndex: number, source: DataType[], rowItemsIndexes: number[]): {
    trimmed: Record<number, boolean>;
    items?: number[];
};
