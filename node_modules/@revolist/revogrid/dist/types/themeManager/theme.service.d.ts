import type { Theme, ThemeConfig, ThemePackage } from '../types/theme';
export declare const DEFAULT_THEME = "default";
export declare const allowedThemes: Theme[];
export default class ThemeService {
    private currentTheme;
    private customRowSize;
    get theme(): ThemePackage;
    get rowSize(): number;
    set rowSize(size: number);
    constructor(cfg: ThemeConfig);
    register(theme: Theme): void;
}
export declare function getTheme(theme?: string | null): Theme;
